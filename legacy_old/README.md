# Legacy prototype (superseded)

Everything in this folder is the original prototype, kept for reference
only - none of it runs (imports from a module that doesn't exist,
deprecated APIs, hardcoded paths from another machine). It has been
replaced by the `assistant/` package at the repo root.

See `docs/ASSISTANT_GUIDE.md` for what replaced each piece, and why.
